This component is designed to read the data form all sensors attached to the board average their values remove erroneous data and monitor for faults with the sensors. 
The communication is achieved via the [[SPI Master component]].
